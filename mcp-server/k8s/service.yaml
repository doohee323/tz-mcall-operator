apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: mcall-system
  labels:
    app: mcp-server
    app.kubernetes.io/name: mcp-server
    app.kubernetes.io/part-of: mcall-operator
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mcp-server

