{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcall-crd.serviceAccountName" . }}
  namespace: {{ include "mcall-crd.namespace" . }}
  labels:
    {{- include "mcall-crd.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}


